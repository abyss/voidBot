exports.config = {
    name: 'Debug',
    enabled: false, // Module enabled on servers by default
    description: 'Debugging commands',
    debug: true, // Commands are only usable by owner
    private: true // Only able to be enabled by bot owner
};
